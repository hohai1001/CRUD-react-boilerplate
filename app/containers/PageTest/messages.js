/*
 * PageTest Messages
 *
 * This contains all the text for the PageTest container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PageTest';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PageTest container!',
  },
});
